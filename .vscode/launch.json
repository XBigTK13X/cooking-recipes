{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Rebuild Site",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/source/main.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}